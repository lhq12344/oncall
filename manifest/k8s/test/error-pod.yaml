apiVersion: v1
kind: Pod
metadata:
  name: error-test-pod
  namespace: infra
  labels:
    app: error-test
    purpose: test
spec:
  containers:
    - name: error-container
      image: non-existent-image-for-testing-only:latest
      command: ["false"]
      resources:
        requests:
          cpu: 10m
          memory: 16Mi
